const { describe, test } = require('node:test');

// Squelette séance 2 : ces tests doivent échouer / rester en attente.
// Implémentation prévue : auth + persistance watchlist + validation.
describe('séance 2 - à implémenter', () => {
  test.todo('inscription puis connexion');
  test.todo('watchlist sans en-tête : 401');
  test.todo('un titre vide est refusé');
});
