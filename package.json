{
  "name": "binggge",
  "version": "1.0.0",
  "private": true,
  "description": "API binggge - le code de l'API vit dans api/",
  "scripts": {
    "install:api": "npm install --prefix api",
    "start": "npm start --prefix api",
    "dev": "npm run dev --prefix api",
    "test": "npm test --prefix api"
  }
}
